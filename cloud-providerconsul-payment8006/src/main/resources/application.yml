server:
  port: 8006

spring:
  application:
    name: cloud-provider-payment

    # Consul 服务中心地址
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}